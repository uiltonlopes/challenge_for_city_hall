<% flash.each do |type, message| %>
  new PNotify({
              title: '<%= notification_color(type) %>',
              text: '<%= message %>',
              type: '<%= notification_color(type) %>',
              styling: 'bootstrap3'
          });
    
    table_team.fnDraw();
<% end %>

<% flash.discard  %>
